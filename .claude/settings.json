{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json;d=json.load(sys.stdin);p=(d.get('tool_input') or {}).get('file_path','');print(json.dumps({'hookSpecificOutput':{'hookEventName':'PostToolUse','additionalContext':'A lecture note, problem set or lab note for a course on the Study Bench (34870 or 34840) was just written: '+p+'. House rule (CLAUDE.md): before this task is finished, extend the Study Bench site (study.madsrudolph.dev) with this material and deploy it. Invoke the study-bench skill for the exact steps (new section + bench + problems + quiz under 5. Semester/Study Bench/courses/<code>/, verify numbers, python3 build.py, npm run deploy:cf, commit and push).'}})) if any(c in p for c in ('34870 Electroacoustics/', '34840 Fundamentals of Acoustics and Noise Control/')) and any(s in p for s in ('/Lecture Notes/', '/Exercises/', '/Labs/')) and p.endswith('.md') else None\" 2>/dev/null || true",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
